# .github/workflows/salvum-github-actions-vps-chile.yml
name: 🇨🇱 Salvum con VPS Chile (Rackeo)

on:
  workflow_dispatch:
    inputs:
      test_mode:
        description: 'Solo test de conexión y login'
        required: false
        default: 'true'
        type: boolean
      agente_especifico:
        description: 'Procesar solo un agente específico'
        required: false
        type: string

  schedule:
    - cron: '0 15 * * 1-5'  # 3pm UTC = 12pm Chile, lunes a viernes

jobs:
  salvum-con-vps-chile:
    runs-on: ubuntu-latest
    
    env:
      SALVUM_USER: ${{ secrets.SALVUM_USER }}
      SALVUM_PASS: ${{ secrets.SALVUM_PASS }}
      GOOGLE_SHEETS_CREDENTIALS: ${{ secrets.GOOGLE_SHEETS_CREDENTIALS }}
      # Credenciales VPS Chile (agregar en GitHub Secrets)
      VPS_HOST: ${{ secrets.VPS_HOST }}
      VPS_PORT: ${{ secrets.VPS_PORT }}
      VPS_USER: ${{ secrets.VPS_USER }}
      VPS_PASSWORD: ${{ secrets.VPS_PASSWORD }}
      TEST_MODE: ${{ github.event.inputs.test_mode }}
      AGENTE_ESPECIFICO: ${{ github.event.inputs.agente_especifico }}
    
    steps:
    - name: 📥 Checkout código
      uses: actions/checkout@v4
    
    - name: 🐍 Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: 🔧 Instalar Chrome
      run: |
        sudo apt-get update
        sudo apt-get install -y wget unzip
        wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
        echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
        sudo apt-get update
        sudo apt-get install -y google-chrome-stable
        google-chrome --version
    
    - name: 📦 Instalar dependencias Python y SSH
      run: |
        pip install --upgrade pip
        pip install selenium webdriver-manager pandas gspread google-auth google-auth-oauthlib google-auth-httplib2 requests
        sudo apt-get install -y sshpass openssh-client
    
    - name: 🌐 Verificar IP original
      run: |
        echo "🌐 IP ORIGINAL (GitHub Actions):"
        curl -s ipinfo.io | jq '.' || curl -s ipinfo.io
    
    - name: 🔑 Configurar SSH para VPS Chile
      run: |
        echo "🔧 Configurando conexión SSH al VPS chileno..."
        
        # Crear directorio SSH
        mkdir -p ~/.ssh
        chmod 700 ~/.ssh
        
        # Desactivar verificación de host (para automación)
        echo "Host *" > ~/.ssh/config
        echo "  StrictHostKeyChecking no" >> ~/.ssh/config
        echo "  UserKnownHostsFile /dev/null" >> ~/.ssh/config
        chmod 600 ~/.ssh/config
        
        # Test de conexión SSH
        echo "🧪 Probando conexión SSH..."
        sshpass -p "$VPS_PASSWORD" ssh -p "$VPS_PORT" "$VPS_USER@$VPS_HOST" \
          -o ConnectTimeout=10 \
          "echo '✅ Conexión SSH exitosa! IP del VPS:'; curl -s ipinfo.io/ip"
    
    - name: 🇨🇱 Crear túnel SOCKS a VPS Chile
      run: |
        echo "🔗 Creando túnel SOCKS al VPS chileno..."
        
        # Crear túnel SOCKS en background
        sshpass -p "$VPS_PASSWORD" ssh -p "$VPS_PORT" \
          -D 8080 -N -f \
          -o ExitOnForwardFailure=yes \
          -o ServerAliveInterval=60 \
          -o ServerAliveCountMax=3 \
          "$VPS_USER@$VPS_HOST"
        
        echo "✅ Túnel SOCKS creado en puerto 8080"
        
        # Verificar que el túnel esté activo
        sleep 5
        if pgrep -f "ssh.*-D 8080" > /dev/null; then
          echo "✅ Túnel SSH está ejecutándose"
          ps aux | grep "ssh.*-D 8080" | grep -v grep
        else
          echo "❌ Error: Túnel SSH no está ejecutándose"
          exit 1
        fi
    
    - name: 🧪 Verificar IP Chilena via SOCKS
      run: |
        echo "🧪 Verificando IP a través del túnel SOCKS..."
        
        # Verificar IP usando el proxy SOCKS
        NEW_IP=$(curl -s --max-time 15 \
          --socks5-hostname localhost:8080 \
          ipinfo.io/json || echo '{"error": "timeout"}')
        
        echo "📍 Nueva IP info (via VPS Chile):"
        echo "$NEW_IP" | jq '.' || echo "$NEW_IP"
        
        # Verificar país
        COUNTRY=$(echo "$NEW_IP" | jq -r '.country // "UNKNOWN"')
        CITY=$(echo "$NEW_IP" | jq -r '.city // "UNKNOWN"')
        IP=$(echo "$NEW_IP" | jq -r '.ip // "UNKNOWN"')
        
        echo "🏢 País detectado: $COUNTRY"
        echo "🏙️ Ciudad: $CITY"
        echo "📡 IP: $IP"
        
        # Verificar que es la IP de nuestro VPS
        if [ "$IP" = "$VPS_HOST" ]; then
          echo "✅ ¡PERFECTO! Usando IP del VPS chileno ($IP)"
        else
          echo "⚠️ Advertencia: IP diferente a la esperada (esperada: $VPS_HOST, obtenida: $IP)"
        fi
        
        if [ "$COUNTRY" = "CL" ]; then
          echo "🇨🇱 ¡ÉXITO! IP chilena confirmada"
        else
          echo "❌ Error: IP no es chilena (País: $COUNTRY)"
          exit 1
        fi
    
    - name: 🔓 Test acceso Salvum via VPS (HTTP + Navegador)
      run: |
        echo "🔍 TEST 1: Probando acceso HTTP con headers realistas..."
        
        # Test con curl + headers de navegador real
        SALVUM_RESPONSE=$(curl -s -I --max-time 20 \
          --socks5-hostname localhost:8080 \
          -H "User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36" \
          -H "Accept: text/html,application/xhtml+xml,application/xml;q=0.9,image/webp,*/*;q=0.8" \
          -H "Accept-Language: es-CL,es;q=0.9,en;q=0.8" \
          -H "Accept-Encoding: gzip, deflate, br" \
          -H "DNT: 1" \
          -H "Connection: keep-alive" \
          -H "Upgrade-Insecure-Requests: 1" \
          https://prescriptores.salvum.cl/login || echo "ERROR: timeout")
        
        echo "📊 Respuesta Salvum (con headers realistas):"
        echo "$SALVUM_RESPONSE"
        
        echo ""
        echo "🤖 TEST 2: Probando con navegador Selenium..."
        
        python -c "
        import time
        from selenium import webdriver
        from selenium.webdriver.chrome.options import Options
        from selenium.webdriver.chrome.service import Service
        from webdriver_manager.chrome import ChromeDriverManager
        
        print('🔧 Configurando Chrome con proxy...')
        options = Options()
        options.add_argument('--headless')
        options.add_argument('--no-sandbox') 
        options.add_argument('--disable-dev-shm-usage')
        options.add_argument('--proxy-server=socks5://localhost:8080')
        options.add_argument('--host-resolver-rules=MAP * ~NOTFOUND , EXCLUDE localhost')
        options.add_argument('--user-agent=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36')
        
        service = Service(ChromeDriverManager().install())
        driver = webdriver.Chrome(service=service, options=options)
        
        try:
            print('🔗 Accediendo a Salvum con navegador...')
            driver.get('https://prescriptores.salvum.cl/login')
            time.sleep(8)
            
            url = driver.current_url
            title = driver.title
            status = 'SUCCESS' if 'salvum' in title.lower() or 'login' in driver.page_source.lower() else 'BLOCKED'
            
            print(f'📍 URL: {url}')
            print(f'📄 Título: {title}')
            print(f'📊 Estado: {status}')
            
            driver.save_screenshot('test_salvum_navegador.png')
            
            if status == 'SUCCESS':
                print('✅ ¡ÉXITO! Salvum accesible con navegador + VPS')
                exit(0)
            else:
                print('❌ Aún bloqueado incluso con navegador')
                if 'bbva' in title.lower():
                    print('🏦 Redirigido a BBVA - bloqueo confirmado')
                exit(1)
                
        except Exception as e:
            print(f'❌ Error: {e}')
            exit(1)
        finally:
            driver.quit()
        "
    
    - name: 🤖 Ejecutar automatización Salvum
      if: ${{ github.event.inputs.test_mode != 'true' }}
      run: |
        echo "🚀 Ejecutando automatización completa de Salvum..."
        echo "🧪 Modo test: ${{ github.event.inputs.test_mode }}"
        echo "👤 Agente específico: ${{ github.event.inputs.agente_especifico }}"
        
        # Configurar proxy para Python/Selenium
        export HTTP_PROXY=socks5://localhost:8080
        export HTTPS_PROXY=socks5://localhost:8080
        
        # Ejecutar automatización
        python salvum_automation_vps.py
      timeout-minutes: 90
    
    - name: 🧪 Solo test de login (modo test)
      if: ${{ github.event.inputs.test_mode == 'true' }}
      run: |
        echo "🧪 Ejecutando test optimizado con VPS Chile..."
        
        python -c "
        import os
        import sys
        import time
        import random
        from selenium import webdriver
        from selenium.webdriver.chrome.options import Options
        from selenium.webdriver.common.by import By
        from selenium.webdriver.chrome.service import Service
        from selenium.webdriver.common.keys import Keys
        from webdriver_manager.chrome import ChromeDriverManager
        
        print('🔧 Configurando navegador optimizado con proxy SOCKS...')
        options = Options()
        options.add_argument('--headless')
        options.add_argument('--no-sandbox')
        options.add_argument('--disable-dev-shm-usage')
        options.add_argument('--disable-gpu')
        options.add_argument('--window-size=1920,1080')
        
        # Proxy SOCKS
        options.add_argument('--proxy-server=socks5://localhost:8080')
        options.add_argument('--host-resolver-rules=MAP * ~NOTFOUND , EXCLUDE localhost')
        
        # Anti-detección avanzada
        options.add_argument('--disable-blink-features=AutomationControlled')
        options.add_argument('--disable-extensions')
        options.add_argument('--disable-plugins')
        options.add_argument('--disable-images')  # Más rápido
        options.add_experimental_option('excludeSwitches', ['enable-automation'])
        options.add_experimental_option('useAutomationExtension', False)
        
        # User agent chileno realista
        options.add_argument('--user-agent=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36')
        
        try:
            service = Service(ChromeDriverManager().install())
            driver = webdriver.Chrome(service=service, options=options)
            
            # Script anti-detección
            driver.execute_script('Object.defineProperty(navigator, \"webdriver\", {get: () => undefined})')
            
            print('🔍 Verificando IP del navegador...')
            driver.get('https://ipinfo.io/json')
            time.sleep(2)
            ip_info = driver.find_element(By.TAG_NAME, 'pre').text
            print(f'📍 IP del navegador: {ip_info}')
            
            # Espera aleatoria para simular comportamiento humano
            wait_time = random.uniform(3, 7)
            print(f'⏳ Esperando {wait_time:.1f}s (comportamiento humano)...')
            time.sleep(wait_time)
            
            print('🔗 Accediendo a Salvum (intento optimizado)...')
            driver.get('https://prescriptores.salvum.cl/login')
            
            # Esperar carga completa
            time.sleep(8)
            
            titulo = driver.title
            url = driver.current_url
            page_source = driver.page_source
            
            print(f'📄 Título: {titulo}')
            print(f'📍 URL: {url}')
            print(f'📊 Tamaño HTML: {len(page_source)} bytes')
            
            # Análisis detallado
            if 'bbva' in titulo.lower():
                print('❌ REDIRIGIDO A BBVA - Detección de datacenter/bot')
                resultado = 'BBVA_REDIRECT'
            elif 'salvum' in page_source.lower() or 'ob forum' in titulo.lower():
                print('✅ ¡ACCESO EXITOSO A SALVUM!')
                resultado = 'SUCCESS'
                
                # Buscar campos de login
                campos_texto = driver.find_elements(By.CSS_SELECTOR, 'input[type=\"text\"], input[type=\"email\"], input[name*=\"usuario\"], input[id*=\"usuario\"]')
                campos_password = driver.find_elements(By.CSS_SELECTOR, 'input[type=\"password\"]')
                
                print(f'🔍 Campos texto encontrados: {len(campos_texto)}')
                print(f'🔍 Campos password encontrados: {len(campos_password)}')
                
                if len(campos_texto) >= 1 and len(campos_password) >= 1:
                    print('✅ FORMULARIO DE LOGIN COMPLETO ENCONTRADO')
                    resultado = 'FULL_SUCCESS'
                    
                    # Probar login si tenemos credenciales
                    usuario = os.getenv('SALVUM_USER')
                    password = os.getenv('SALVUM_PASS')
                    
                    if usuario and password:
                        print('🔐 Probando login real...')
                        try:
                            campos_texto[0].clear()
                            time.sleep(1)
                            campos_texto[0].send_keys(usuario)
                            time.sleep(1)
                            campos_password[0].clear()
                            time.sleep(1)
                            campos_password[0].send_keys(password)
                            time.sleep(1)
                            campos_password[0].send_keys(Keys.RETURN)
                            
                            # Esperar respuesta
                            time.sleep(10)
                            
                            new_url = driver.current_url
                            new_title = driver.title
                            
                            print(f'📍 URL post-login: {new_url}')
                            print(f'📄 Título post-login: {new_title}')
                            
                            if new_url != url or 'dashboard' in new_url.lower():
                                print('🎉 ¡LOGIN EXITOSO!')
                                resultado = 'LOGIN_SUCCESS'
                                driver.save_screenshot('salvum_login_exitoso.png')
                            else:
                                print('⚠️ Login sin cambio de URL')
                                driver.save_screenshot('salvum_login_sin_cambio.png')
                                
                        except Exception as e:
                            print(f'⚠️ Error en login: {e}')
                    else:
                        print('ℹ️ Sin credenciales para test de login')
                else:
                    print('⚠️ Formulario incompleto')
            else:
                print('❓ Contenido desconocido')
                resultado = 'UNKNOWN'
            
            # Guardar evidencia
            driver.save_screenshot('test_salvum_optimizado.png')
            with open('salvum_optimizado.html', 'w', encoding='utf-8') as f:
                f.write(page_source)
            
            print(f'📊 RESULTADO FINAL: {resultado}')
            
            if resultado in ['SUCCESS', 'FULL_SUCCESS', 'LOGIN_SUCCESS']:
                print('🎉 ¡ÉXITO! VPS + Navegador optimizado funciona')
                exit(0)
            else:
                print('❌ Aún hay problemas que resolver')
                exit(1)
            
        except Exception as e:
            print(f'❌ Error en test: {e}')
            if 'driver' in locals():
                driver.save_screenshot('error_salvum_optimizado.png')
                with open('error_optimizado.html', 'w', encoding='utf-8') as f:
                    f.write(driver.page_source)
            sys.exit(1)
        finally:
            if 'driver' in locals():
                driver.quit()
        "
    
    - name: 🔌 Cerrar túnel SSH
      if: always()
      run: |
        echo "🔌 Cerrando túnel SSH..."
        
        # Matar procesos SSH del túnel
        pkill -f "ssh.*-D 8080" || echo "Túnel SSH ya estaba cerrado"
        
        # Verificar desconexión
        sleep 3
        FINAL_IP=$(curl -s --max-time 10 ipinfo.io/json || echo '{"error": "timeout"}')
        echo "📍 IP final (sin túnel):"
        echo "$FINAL_IP" | jq '.' || echo "$FINAL_IP"
    
    - name: 📊 Subir resultados
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: salvum-vps-chile-results-${{ github.run_number }}
        path: |
          *.png
          *.json
          *.html
        retention-days: 7
    
    - name: 📋 Resumen final
      if: always()
      run: |
        echo "📋 RESUMEN EJECUCIÓN CON VPS CHILE"
        echo "=================================="
        echo "🕐 Timestamp: $(date)"
        echo "🖥️ VPS Host: $VPS_HOST"
        echo "🔌 VPS Port: $VPS_PORT"
        echo "👤 VPS User: $VPS_USER"
        echo "🧪 Modo test: ${{ github.event.inputs.test_mode }}"
        echo "👤 Agente específico: ${{ github.event.inputs.agente_especifico }}"
        echo ""
        echo "📁 Archivos generados:"
        ls -la *.png *.json *.html 2>/dev/null || echo "No se generaron archivos"
        echo ""
        echo "💡 VENTAJAS DE TU VPS CHILE:"
        echo "   ✅ IP 100% chilena garantizada (45.7.230.109)"
        echo "   ✅ Servidor dedicado propio"
        echo "   ✅ Sin límites de uso"
        echo "   ✅ Mayor velocidad que VPN shared"
        echo "   ✅ Más confiable y estable"
        echo "   ✅ Sin restricciones de proveedores VPN"
        echo "=================================="
